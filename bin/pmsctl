#!/usr/bin/env python3
"""Punto de entrada ejecutable de la herramienta pmsctl.

Este fichero mantiene el ejecutable lo más pequeño posible. Toda la lógica real
vive dentro del paquete ``pmsctl`` para facilitar pruebas y futuras ampliaciones.
"""

import os
import sys


RAIZ_PROYECTO = os.path.abspath(os.path.join(os.path.dirname(__file__), os.pardir))
if RAIZ_PROYECTO not in sys.path:
    sys.path.insert(0, RAIZ_PROYECTO)

from pmsctl.cli import main  # noqa: E402


if __name__ == "__main__":
    sys.exit(main())
